
streamlit
torch
torchvision  # Required for image processing (e.g., PIL compatibility)
transformers
accelerate  # Required for device_map usage (fixes your error) [[1]]
pillow  # Required for PIL image handling
psutil  # Required for memory monitoring
